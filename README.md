HTML & CSS Fundamentals

A collection of foundational web development exercises covering HTML5 elements, CSS styling techniques, and layout structures. This repository serves as a personal reference for core frontend concepts.

📂 Project Structure

The files in this repository are organized by topic:

HTML Basics: Text formatting (`text.html`), lists (`List.html`), and links (`link.html`).
Layout & Structure: Tables (`Table.html`, `CSS table1.html`), Divs & Spans (`div and span.html`), and Iframes (`iframe.html`).
Styling (CSS): Examples of Inline, Internal, and External CSS, plus ID/Class selectors.
Interactive Elements: Dropdown menus (`dropdown.html`) and Forms (`froms.html`).
Media: Handling images (`image.html`, `apple.jpg`).

🚀 Key Concepts Covered

Document Structure: semantic HTML5 tagging.
CSS Integration:Three ways to apply styles to a webpage.
Data Representation:Building and styling complex tables.
User Input:Creating functional forms and dropdown selections.

🛠️ How to Use
1. Clone the repository.
2. Open any `.html` file in your preferred web browser to see the output.
3. Edit the code in a text editor (like VS Code) to experiment with the styles.
